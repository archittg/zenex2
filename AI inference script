from ultralytics import YOLO

# Load a lightweight YOLO model (e.g., YOLOv8n)
model = YOLO("yolov8n.pt")

# Run inference on a sample image (simulate satellite capture)
results = model("path_to_sample_image.jpg")
